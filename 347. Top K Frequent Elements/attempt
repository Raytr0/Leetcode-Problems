class Solution(object):
    def topKFrequent(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: List[int]
        """
    #Start with a dictonary
    #count and store numbers and how many times it has been recorded
    #return top after sorting